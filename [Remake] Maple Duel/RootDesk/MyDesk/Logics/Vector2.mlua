@Logic
script Vector2 extends Logic

	method Vector2 Lerp(Vector2 v1, Vector2 v2, number t)
		-- 두 벡터 사이를 선형 보간하여 새로운 벡터 반환
		return Vector2(v1.x * (1 - t) + v2.x * t, v1.y * (1 - t) + v2.y * t)
	end

end