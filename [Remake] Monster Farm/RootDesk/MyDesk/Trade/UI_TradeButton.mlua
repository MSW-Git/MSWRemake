@Component
script UI_TradeButton extends Component

	@ExecSpace("ClientOnly")
	method void OnClick()
		--===========================================
		-- 서버 지연 대응 로직
		--===========================================
		local player = _UserService.LocalPlayer
		
		if player.PlayerData.IsTransferring then
			return
		end
		--===========================================
		
		
		-- 버튼을 누르면 다른 플레이어에게 교환 신청을 보냅니다. --
		local owner = player.PlayerData.InteractedPlayer
		
		if not isvalid(owner) or not isvalid(player) then
			return
		end
		
		--=======================================================================
		-- 최대 몬스터 보유 숫자에 도달했다면
		--=======================================================================
		if #player.PlayerData.Minventory_Id >= _PlayerService.MaxHaveMonster then
			_PlayerService:MonsterSpaceFullAlertByTrade()
			return
		end
		--=======================================================================
		
		--=======================================================================
		-- 최대 몬스터 보유 숫자에 도달했다면
		--=======================================================================
		if #owner.PlayerData.Minventory_Id >= _PlayerService.MaxHaveMonster then
			_UIToast:ShowMessage(_LocalizationService.LocalTranslator:GetText("Popup_imPossibleToTrade"))
			return
		end
		--=======================================================================
		
		self.Entity.ButtonComponent.Enable = false
		
		_TradeService:MakeTrade(player, owner)
	end

	@ExecSpace("ClientOnly")
	@EventSender("Self")
	handler HandleButtonClickEvent(ButtonClickEvent event)
		-- Parameters
		local Entity = event.Entity
		--------------------------------------------------------
		self:OnClick()
	end

end