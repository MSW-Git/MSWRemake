@Component
script AchivementItem extends Component

	@HideFromInspector
	property number id = 0

	@ExecSpace("ClientOnly")
	@EventSender("Self")
	handler HandleButtonClickEvent(ButtonClickEvent event)
		_AchievementService:ClickButton(self.id, self.Entity) -- 클릭 시 해당 업적을 완료하고 보상을 받습니다.
	end

end