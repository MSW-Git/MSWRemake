@Component
script CH10004 extends ChallengeComponent

	@ExecSpace("ServerOnly")
	method void StartGameHandler(any event)
		__base:StartGameHandler(event)
		
		-- 아이템 상점 이용 불가
		-- '조교 챙' 룬카드를 보유한 상태로 시작 (적 처치 시, 5% 확률로 무작위 아이템 획득)
		---------------------------------------------------------
		local user = self.Entity.Parent
		local specialShopManager = user.SpecialShopManager
		local runeParent = user:GetChildByName("RuneCardParent")
		local userPControl = user.TM_PropertyControl
		local runeID = "RC20017"
		----------------------------------------------------------------
		userPControl.Shop_CanPurchaseItems = false
		
		specialShopManager:GetRuneCard_Check(runeID)
	end

	@ExecSpace("ServerOnly")
	method void StartStageHandler(any event)
		__base:StartStageHandler(event)
		
		-- 스테이지마다 '길 잃은 병정' 룬 카드 획득
		-- 자리가 없으면 불가
		---------------------------------------------------------
		local user = self.Entity.Parent
		local specialShopManager = user.SpecialShopManager
		local runeParent = user:GetChildByName("RuneCardParent")
		local userPControl = user.TM_PropertyControl
		local runeID = "RC20015"
		----------------------------------------------------------------
		specialShopManager:GetRuneCard_Check(runeID)
	end

end