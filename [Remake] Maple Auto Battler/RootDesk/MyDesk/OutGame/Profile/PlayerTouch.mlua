@Component
script PlayerTouch extends Component

	@EventSender("Self")
	handler HandleTouchEvent(TouchEvent event)
		-- 플레이어 터치 이벤트 핸들러 (현재 비활성화)
		---- Parameters
		--local TouchId = event.TouchId
		---- local TouchPoint = event.TouchPoint
		-----------------------------------------------------------
		--log("touchID = ", TouchId)
		--if TouchId == -2 then
		--    self.Entity:SendEvent(PlayerTouchEvent(self.Entity))
		--end
	end

end