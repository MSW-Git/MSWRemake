@Event
script SideMenuSettingChangedEvent extends EventType

	property integer SideMenuId = 0

	method void Init(integer sideMenuId)
		-- 사이드메뉴 설정 변경 이벤트를 초기화하는 함수
		self.SideMenuId = sideMenuId
	end

end