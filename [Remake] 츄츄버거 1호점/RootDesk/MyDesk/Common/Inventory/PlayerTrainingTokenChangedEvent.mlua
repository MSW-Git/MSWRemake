@Event
script PlayerTrainingTokenChangedEvent extends EventType

	property integer Current = 0

	property integer Max = 0

	method void Init(integer current, integer max)
		-- 플레이어 훈련 토큰 변경 이벤트를 초기화하는 메서드
		self.Current = current
		self.Max = max
	end

end