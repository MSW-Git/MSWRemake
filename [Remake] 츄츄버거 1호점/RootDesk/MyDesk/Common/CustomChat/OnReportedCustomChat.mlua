@Event
script OnReportedCustomChat extends EventType

	property integer index = 0

	property string senderProfileCode = ""

	method void Init(integer index, string senderProfileCode)
		-- 신고된 채팅 데이터를 초기화하는 함수
		self.index = index
		self.senderProfileCode = senderProfileCode
	end

end