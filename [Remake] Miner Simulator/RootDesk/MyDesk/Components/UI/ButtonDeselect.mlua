@Component
script ButtonDeselect extends Component

	@EventSender("Self")
	handler HandleButtonClickEvent(ButtonClickEvent event)
		-- 버튼 클릭 시 부모의 선택 상태를 해제
		-- Parameters
		local Entity = event.Entity
		--------------------------------------------------------
		self.Entity.Parent.ButtonSelect:Select(false)
	end

end