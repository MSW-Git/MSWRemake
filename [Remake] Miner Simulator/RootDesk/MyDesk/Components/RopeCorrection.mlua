@Component
script RopeCorrection extends Component

	@ExecSpace("ClientOnly")
	method void OnBeginPlay()
		-- 로프 충돌 박스 크기 보정
		wait(1)
		self.Entity.ClimbableComponent.BoxSize.x = self.Entity.ClimbableComponent.BoxSize.x + 0.5
	end

end