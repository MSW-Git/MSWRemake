@Component
script TrapGreatRockUI extends Component

	@EventSender("Service", "InputService")
	handler HandleKeyDownEvent(KeyDownEvent event)
		-- 키보드 입력을 위한 핸들러 (현재 구현되지 않음)
		-- Parameters
		local key = event.key
		--------------------------------------------------------
		
	end

end