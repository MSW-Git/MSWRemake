@Component
script LobbyInput extends Component

	@EventSender("Service", "InputService")
	handler HandleKeyDownEvent(KeyDownEvent event)
		-- 로비에서 키보드 입력을 처리 (스페이스바로 대시)
		
		local key = event.key
		
		if key == KeyboardKey.Space then
			_UserService.LocalPlayer.LobbyPlayer:Dash()
		end
	end

end