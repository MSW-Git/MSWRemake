@Component
script InstantaneousDeath extends Component

	property number percent = 0

	method void OnBeginPlay()
		-- 즉사 효과 컴포넌트 초기화
		if isvalid(self.Entity.CurrentMap.MultiBattleMap) then
		end
	end

end