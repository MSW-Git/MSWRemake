@Component
script EquipmentListSlotButton extends Component

	property string uniqueKey = ""

	@ExecSpace("ClientOnly")
	@EventSender("Self")
	handler HandleButtonClickEvent(ButtonClickEvent event)
		-- 장비 슬롯 버튼 클릭 이벤트 처리
		
		local Entity = event.Entity
	end

end