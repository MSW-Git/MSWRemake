@Event
script BestEarningRecordChangedEvent extends EventType

	property integer Record = 0

	property integer ChangeAmount = 0

	method void Init(integer record, integer changeAmount)
		-- 최고 수익 기록 변경 이벤트 데이터를 초기화하는 함수
		self.Record = record
		self.ChangeAmount = changeAmount
	end

end