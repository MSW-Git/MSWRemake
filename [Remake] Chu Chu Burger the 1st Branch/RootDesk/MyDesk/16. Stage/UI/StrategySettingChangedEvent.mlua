@Event
script StrategySettingChangedEvent extends EventType

	property integer StrategyId = 0

	property integer Level = 0

	method void Init(integer strategyId, integer level)
		-- 전략 설정 변경 이벤트를 초기화하는 함수
		self.StrategyId = strategyId
		self.Level = level
	end

end