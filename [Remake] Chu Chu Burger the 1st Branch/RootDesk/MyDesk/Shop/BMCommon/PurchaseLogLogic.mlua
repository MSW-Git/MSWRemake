@Logic
script PurchaseLogLogic extends Logic

	property string EmptyString = "empty"

	property string SpecialShopSource = "SpecialShop"

	property string BoosterPackSource = "BoosterPack"

	property string PiggyBankSource = "PiggyBank"

	property string StagePassSource = "StagePass"

	property string UnauthorizedFailMsg = " Unauthorized attempt detected."

	property string WorldCoinString = "WorldCoin"

	property string DiamondString = "Diamond"

	property string DiamondLackFailMsg = "The purchase failed due to a lack of diamonds."

	@ExecSpace("ServerOnly")
	method void PurchaseSuccessLog(string userId, string productName, string assetBefore, string assetAfter, string productCount, string canBuyCount, table reward, string nickname, string category, string productId, string price, string source, string assetId, string purchaseId, string playerStageId)
		-- 구매 성공 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseSuccessLog_SpecialShop_WorldCoin(any _purchaseInfo, SpecialShopProductData productData)
		-- 스페셜 샵 월드코인 구매 성공 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseSuccessLog_SpecialShop_Diamond(string userId, SpecialShopProductData productData, string count)
		-- 스페셜 샵 다이아몬드 구매 성공 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseSuccessLog_StagePass(any _purchaseInfo, StagePassProductData productData)
		-- 스테이지 패스 구매 성공 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseSuccessLog_PiggyBank(any _purchaseInfo, PiggyBankLevelData levelData)
		-- 피기뱅크 구매 성공 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseSuccessLog_BoosterPack(string userId, string boosterPackItemId, integer boosterPackPrice)
		-- 부스터 팩 구매 성공 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseFailLog(string userId, string nickname, string failMessage, string category, string productId, string price, string source, string assetId, string purchaseId, string playerStageId)
		-- 구매 실패 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseFailLog_InvalidEntity(any _purchaseInfo)
		-- 잘못된 엔티티로 인한 구매 실패 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseFailLog_FailOnPrompt(string productId, string purchaseId, string userId)
		-- 구매 프롬프트 실패 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseFailLog_InvalidProductId(any _purchaseInfo)
		-- 잘못된 상품 ID로 인한 구매 실패 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseFailLog_UnauthorizedPiggyBank(any _purchaseInfo, string frontMsg)
		-- 피기뱅크 무단 구매 시도 실패 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseFailLog_UnauthorizedBoosterPack(string userId, string frontMsg, string boosterPackItemId, integer boosterPackPrice)
		-- 부스터 팩 무단 구매 시도 실패 로그 기록
		
	end

	@ExecSpace("Server")
	method void PurchaseFailLog_DiamondLack(string userId, string itemId, integer totalPrice)
		-- 다이아몬드 부족으로 인한 구매 실패 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PurchaseFailLog_SpecialShop_AddStorageFail(any _purchaseInfo, SpecialShopProductData productData)
		-- 스페셜 샵 인벤토리 추가 실패 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void StagePassCompleteLog(StagePassRewardData rewardData, string userId, StagePassProductData productData)
		-- 스테이지 패스 보상 수령 완료 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PiggyBankLevelUpRewardLog(PiggyBankLevelUpRewardData rewardData, string userId)
		-- 피기뱅크 레벨업 보상 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void PiggyBankFlowLog(string userId, integer beforePoint, integer addedPoint, integer afterPoint, string flowType)
		-- 피기뱅크 포인트 흐름 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void StorageFlowLog(any _purchaseInfo, string actionType)
		-- 인벤토리 흐름 로그 기록
		
	end

	@ExecSpace("ServerOnly")
	method void AllLoggingForTest(any _purchaseInfo)
		-- 테스트용 전체 로깅
		
	end

	@ExecSpace("Server")
	method void CheckCanTimeFlowsLog(string userId, boolean canTimeFlowsClient, integer lastReceievedClientPacketTime, integer responseLimitTime, integer utcTime)
		-- 클라이언트 시간 흐름 검증 로그 기록
		
	end

end