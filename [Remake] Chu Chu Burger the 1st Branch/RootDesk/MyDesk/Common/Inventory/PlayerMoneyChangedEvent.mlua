@Event
script PlayerMoneyChangedEvent extends EventType

	property integer Current = 0

	property integer Amount = 0

	method void Init(integer current, integer amount)
		-- 플레이어 돈 변경 이벤트를 초기화하는 메서드
		self.Current = current
		self.Amount = amount
	end

end