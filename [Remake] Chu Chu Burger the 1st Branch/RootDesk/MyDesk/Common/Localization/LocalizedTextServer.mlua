@Struct
script LocalizedTextServer

	property string key = ""

	property SyncTable<string, string> localeText

	method string GetText(string localeId)
		-- 지정된 로케일 ID에 해당하는 현지화된 텍스트를 반환
		return self.localeText[localeId]
	end

end