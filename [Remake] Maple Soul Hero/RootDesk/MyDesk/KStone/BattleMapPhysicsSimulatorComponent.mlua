@Component
script BattleMapPhysicsSimulatorComponent extends PhysicsSimulatorComponent

	@EventSender("Self")
	handler HandleTimerChange(TimerChange event)
		-- 타이머 상태 변경에 따른 물리 시뮬레이션 일시정지/재개
		self.Paused = event.pause
	end

end