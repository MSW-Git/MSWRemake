@Logic
script GameSettingLogic extends Logic

	@HideFromInspector
	property integer mode = 0

	@ExecSpace("Client")
	method void SetMode(integer mode)
		-- 게임 모드를 설정하고 사용하지 않는 리소스를 정리
		self.mode = mode
		_ResourceService:UnloadUnusedResources(0)
	end

end