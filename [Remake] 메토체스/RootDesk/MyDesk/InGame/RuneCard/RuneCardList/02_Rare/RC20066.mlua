@Component
script RC20066 extends RuneCardComponent

	@ExecSpace("Server")
	method void BuyThisRuneCard(string runeID)
		__base:BuyThisRuneCard(runeID)
		
		-- 방해꾼 시너지 효과
		-- 방해꾼의 가면을 장착한 캐릭터와 같은 행에서 전투를 시작하는 적 유닛들이 마나 과부하 상태가 됩니다.
		-- 방해꾼의 가면 효과의 은신 지속시간이 50% 증가합니다.
		--------------------------------------------------
		local user = self.Entity.Parent.Parent
		local synergyManager = user.SynergyManager_New
		--------------------------------------------------
		if self.SetTeamType == "User" then
			synergyManager.SynergyRuneOn["S10011"] += 1
		else
			synergyManager.Enemy_SynergyRuneOn["S10011"] += 1
		end
	end

	@ExecSpace("Server")
	method void SellThisRuneCard(string runeID)
		-- 방해꾼 시너지 효과
		-- 방해꾼의 가면을 장착한 캐릭터와 같은 행에서 전투를 시작하는 적 유닛들이 마나 과부하 상태가 됩니다.
		-- 방해꾼의 가면 효과의 은신 지속시간이 50% 증가합니다.
		--------------------------------------------------
		local user = self.Entity.Parent.Parent
		local synergyManager = user.SynergyManager_New
		--------------------------------------------------
		if self.SetTeamType == "User" then
			synergyManager.SynergyRuneOn["S10011"] -= 1
		else
			synergyManager.Enemy_SynergyRuneOn["S10011"] -= 1
		end
		
		__base:SellThisRuneCard(runeID)
	end

	@ExecSpace("ServerOnly")
	method void SetActiveHandler(any event)
		-------------------------------------------------------------
		local user = self.Entity.Parent.Parent
		local synergyManager = user.SynergyManager_New
		local synergyID = "S10011"
		local active = event.active
		-------------------------------------------------------------
		if active ~= self.OnActive then
			if active then
				-- 활성화 시
				synergyManager.SynergyRuneOn[synergyID] += 1
			else
				-- 비활성화 시
				synergyManager.SynergyRuneOn[synergyID] -= 1
			end
		end
		
		
		__base:SetActiveHandler(event)
	end

end